I really liked the idea ZlothY had but they sorta executed it poorly, so this project will have:

Cushions & Stuff,

Dynamic Lighting (Using shaders! I'll probably just get the brightest pixel of the video and then use that to add fake lighting to the scene)

I'd prefer having it in a place that cant really be changed by gtag, so it will be a custom scene instead.

Network syncing with basic websockets or something (we will use URLs instead of videos so :3)

I'll also have cute music playing by default, I was kinda sad when making this :<